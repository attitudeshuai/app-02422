<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.blog.mapper.ArticleFavoriteMapper">

    <resultMap id="ArticleVOMap" type="com.blog.vo.ArticleVO">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="username" property="username"/>
        <result column="nickname" property="nickname"/>
        <result column="category_id" property="categoryId"/>
        <result column="category_name" property="categoryName"/>
        <result column="title" property="title"/>
        <result column="content" property="content"/>
        <result column="cover_image" property="coverImage"/>
        <result column="view_count" property="viewCount"/>
        <result column="like_count" property="likeCount"/>
        <result column="favorite_count" property="favoriteCount"/>
        <result column="comment_count" property="commentCount"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="selectFavoriteArticlesByUserId" resultMap="ArticleVOMap">
        SELECT 
            a.id, a.user_id, a.category_id, a.title, a.content, a.cover_image,
            a.view_count, a.like_count, a.favorite_count, a.comment_count, a.status,
            a.create_time, a.update_time,
            u.username, u.nickname,
            c.name as category_name
        FROM article_favorite af
        INNER JOIN article a ON af.article_id = a.id AND a.deleted = 0 AND a.status = 1
        LEFT JOIN user u ON a.user_id = u.id
        LEFT JOIN category c ON a.category_id = c.id
        WHERE af.user_id = #{userId}
        ORDER BY af.create_time DESC
    </select>

</mapper>
